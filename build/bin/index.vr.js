import React, { Component } from 'react';
import {
AppRegistry,
asset,
View,
} from 'react-vr';

export default class bin extends Component {
render() {
return (
<View>
    <View/>
    <View/>
</View>
);
}
};

AppRegistry.registerComponent('bin', () => bin);
